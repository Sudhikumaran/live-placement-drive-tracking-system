# Render Deployment Environment Variables
# Copy these to Render dashboard and set actual values

NODE_ENV=production
PORT=10000

# MongoDB Atlas - Create a cluster and get connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/placement-db?retryWrites=true&w=majority

# JWT Secret - Generate a random 32+ character string
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-here

# Client URL - Your Render frontend domain
CLIENT_URL=https://placement-tracker.onrender.com

# Logging
LOG_LEVEL=info

# Email Service (if using NodeMailer)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
